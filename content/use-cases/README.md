# Use cases moved to Sanity

These pages used to live here as markdown. They were ingested as `aiAdoptionUseCase` documents and now live in Sanity Studio.

The website serves `/ai-adoption/<domain>/<stage>/<slug>` from Sanity, not from this directory.

Do not add use-case markdown here. Edit the documents in Studio.
